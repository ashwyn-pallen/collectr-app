<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Post - Collectr</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
<link rel="stylesheet" href="/styles/navbar.css" />
</head>
<body class="bg-dark d-flex flex-column min-vh-100">

  <%- include('partials/navbar') %>

  <form action="/posts/<%= post.id %>/edit" method="POST" class="container mt-5 p-4 rounded text-white" style="max-width: 500px;">
    <h2 class="mb-4 text-center" id="formHeader">Edit Post</h2>

    <div class="mb-3">
      <label for="title" class="form-label">Title</label>
      <input type="text" name="title" class="form-control" id="title" value="<%= post.title %>" required />
    </div>

    <div class="mb-3">
      <label for="description" class="form-label">Description</label>
      <input type="text" name="description" class="form-control" id="description" value="<%= post.postDescription %>" />
    </div>

    <div class="mb-3">
      <label for="image" class="form-label">Image URL</label>
      <input type="text" name="image" class="form-control" id="image" value="<%= post.image %>" />
    </div>

    <button type="submit" class="btn w-100" id="buttonStyl">Update Post</button>

    <div class="mt-3 text-center">
      <a href="/collection/<%= post.collection_id %>" class="btn btn-outline-light btn-sm">Back to Collection</a>
    </div>
  </form>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
