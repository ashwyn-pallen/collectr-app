<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Collectr Dashboard</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>

  <!-- Custom Styles -->
  <link rel="stylesheet" href="../styles/navbar.css">
  <link rel="stylesheet" href="../styles/dashboard.css">
</head>
<body class="bg-dark text-white d-flex flex-column min-vh-100">

  <%- include('partials/navbar') %>

<main class="container mt-5 flex-fill">
  <div class="text-center mb-4">
    <h1 class="display-4">Dashboard</h1>
      <br>
      <br>
    <% if (user.profilePicture) { %>
      <img src="<%= user.profilePicture %>" alt="Profile Picture" class="rounded-circle my-3" style="width: 150px; height: 150px; object-fit: cover; border: 3px solid #fff;">
    <% } else { %>
      <div class="my-3">
        <span class="text-muted">No profile picture uploaded</span>
      </div>
    <% } %>

    <h2 class="h4">Welcome, <%= user.username %>!</h2>
  </div>

  <div class="d-grid gap-3 col-md-6 mx-auto">
       <a href="/collections" class="btn btn-primary w-100">View Collections</a>
    <a href="/users/edit" class="btn btn-outline-light w-100">Edit Profile</a>

    <form action="/logout" method="get">
      <button type="submit" class="btn btn-danger w-100">Log Out</button>
    </form>
  </div>
</main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>